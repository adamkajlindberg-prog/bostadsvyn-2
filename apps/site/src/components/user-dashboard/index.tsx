"use client";

// This component has been refactored - the dashboard now uses subroutes
// The header and navigation are in dashboard-header and dashboard-nav components
// Individual pages are in app/(pages)/(default-layout)/dashboard/*/page.tsx

// Keeping this file for backward compatibility, but it's no longer used
// All functionality has been migrated to the new subroute structure

export default function UserDashboard() {
  return null;
}
